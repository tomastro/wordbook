{
  "name": "wordbook-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "AI-powered desktop study companion for Word Collector",
  "main": "main.cjs",
  "scripts": {
    "start": "electron .",
    "smoke": "electron . --smoke-test",
    "check": "node --check main.cjs && node --check preload.cjs && node --check lib/core.cjs && node --check renderer/app.js",
    "test": "node --test test/*.test.cjs"
  },
  "devDependencies": {
    "electron": "^44.2.0"
  }
}
